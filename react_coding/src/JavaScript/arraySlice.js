const numbers = [10, 20, 30, 40, 50]

const middleThree = numbers.slice(1, 4)
console.log(middleThree)

const numbs = numbers.slice()
console.log(numbs)
const numbs1 = numbers.slice(1)
console.log(numbs1)
const numbs2 = numbers.slice(-1)
console.log(numbs2)
const numbs3 = numbers.slice(-2)
console.log(numbs3)